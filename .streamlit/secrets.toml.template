# Copy this file to secrets.toml and fill in your actual values
# Never commit the actual secrets.toml file to version control

GEMINI_API_KEY = "your-google-gemini-api-key-here"
APP_PASSWORD = "your-admin-password-here"

# Instructions:
# 1. Get your Gemini API key from https://makersuite.google.com/app/apikey
# 2. Set a secure admin password for the Admin Rate Panel
# 3. Rename this file to secrets.toml
# 4. Never commit secrets.toml to git (it's already in .gitignore)